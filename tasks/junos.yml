---
# Log in and run the commands specified in the group_vars/ files.
- name: "JUNOS >> Gather Juniper JUNOS information"
  junipernetworks.junos.junos_command:
    commands: "{{ command_list }}"
  register: "CLI_OUTPUT"
...
